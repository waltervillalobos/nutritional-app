import { ComingSoon } from '../../src/components/ComingSoon';

/** Recipe Catalog (US-08) — placeholder until its own story is implemented. */
export default function RecipesScreen() {
  return <ComingSoon feature="Recetas" />;
}
