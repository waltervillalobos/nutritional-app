import { ComingSoon } from '../../src/components/ComingSoon';

/** Today's Plan (US-04) — placeholder until its own story is implemented. */
export default function TodayScreen() {
  return <ComingSoon feature="Plan de hoy" />;
}
