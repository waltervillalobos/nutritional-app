import { ComingSoon } from '../../src/components/ComingSoon';

/** Food Database (US-11) — placeholder until its own story is implemented. */
export default function FoodsScreen() {
  return <ComingSoon feature="Alimentos" />;
}
