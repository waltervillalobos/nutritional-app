# Expo/React Native packages declare strict peer ranges that npm 7+ cannot always
# satisfy across the SDK's curated set. Expo validates compatibility via
# `expo install` / `expo-doctor`, not npm peer ranges, so we relax npm's strict
# peer resolution to keep installs deterministic (and the devcontainer's
# postCreateCommand reliable).
legacy-peer-deps=true
